# Dependency directories
# This is the most important line. It prevents the large node_modules folder
# from being uploaded to GitHub. Each user should run `npm install` to generate it locally.
node_modules/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Environment variables
# If you ever decide to store sensitive data like database passwords,
# you should put them in a .env file and ignore it.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# macOS specific files
.DS_Store

# Windows specific files
Thumbs.db
